<%= form_for(@persona_question, :html => { :class => "form-horizontal", :role => "form" }) do |f| %>
  <% if @persona_question.errors.any? %>
    <div class="alert alert-danger alert-dismissable" role="alert">
      <button type="button" class="close" data-dismiss="alert"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
      <h4><%= pluralize(@persona_question.errors.count, "error") %> prohibited this persona_question from being saved:</h4>

      <ul>
      <% @persona_question.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :persona, :class => "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.text_field :persona, :class => "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :question, :class => "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.text_field :question, :class => "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
      <%= f.submit :class => "btn btn-primary" %>
    </div>
  </div>
<% end %>
